{
  "name": "discord-webhook-middleware",
  "version": "1.0.0",
  "description": "Middleware to forward GitHub webhooks to Discord (works for fork PRs)",
  "main": "api/webhook.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "keywords": [
    "github",
    "discord",
    "webhook",
    "middleware"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.x"
  }
}

